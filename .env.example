# Copy to .env.local for local dev. Set the same values in Netlify's
# environment variables (Site settings > Environment variables).
VITE_ANVIL_BASE=https://hmxlive-contract-creator.anvil.app/_/api
VITE_TOOLBOX_KEY=replace-with-your-toolbox_shared_secret
